# 数据类型

本文档汇总 Trait / Command 参数中引用的自定义数据类型（枚举、结构体等）。

## ActiveHardwareFaultsEnum32

该枚举定义 ActiveHardwareFaults Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Unspecified | 未知故障 |
| 0x01 | Radio | 无线电故障 |
| 0x02 | Sensor | 传感器故障 |
| 0x03 | ResettableOverTemp | 过热保护 |
| 0x04 | NonResettableOverTemp | 过热异常 |
| 0x05 | PowerSource | 电源故障 |
| 0x06 | VisualDisplayFault | 显示故障 |
| 0x07 | AudioOutputFault | 音频输出故障 |
| 0x08 | UserInterfaceFault | 界面显示故障 |
| 0x09 | NonVolatileMemoryError | 内存故障 |
| 0x0A | TamperDetected | 设备被拆卸/篡改 |
| 0xFF | Normal | 无故障 |

## ActivityDetectionModeSettingEnum32

该枚举定义 ActivityDetectionModeSetting Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | HumanPresence | 人体存在 |
| 0x03 | PersonnelLocation | 人员定位 |
| 0x05 | FallDetection | 跌倒侦测 |
| 0x08 | CeilingFallSingleTracking | 顶装跌倒和单人定位 |
| 0x09 | SleepMode | 睡眠 |
| 0x11 | GestureControl | 手势 |

## AddOrUpdateThreadNetwork Command Structure

AddOrUpdateThreadNetwork Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | OperationalDataset | octetString | M | 参数。 |

## AddOrUpdateThreadNetworkResponse Command Structure

AddOrUpdateThreadNetworkResponse Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | StatusCodes | uint8 | M | 参数。 |

## AddOrUpdateWiFiNetwork Command Structure

AddOrUpdateWiFiNetwork Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | SSID | characterString | O | 参数。 |
| 1 | Credentials | characterString | O | 参数。 |
| 2 | Encryption | uint8 | O | 参数。 |
| 3 | CountryDomain | characterString | O | 参数。 |
| 4 | Bindkey | characterString | O | 参数。 |
| 5 | TimeZone | characterString | O | 参数。 |

## AddOrUpdateWiFiNetworkResponse Command Structure

AddOrUpdateWiFiNetworkResponse Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | StatusCodes | uint8 | M | 参数。 |

## AddOrUpdateZigbeeNetwork Command Structure

AddOrUpdateZigbeeNetwork Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Channel | uint8 | M | 参数。 |
| 1 | ExtendedPANID | octetString | M | 参数。 |
| 2 | NetworkKey | octetString | M | 参数。 |
| 3 | NetworkName | characterString | O | 参数。 |
| 4 | PANID | uint16 | M | 参数。 |

## AddOrUpdateZigbeeNetworkResponse Command Structure

AddOrUpdateZigbeeNetworkResponse Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | StatusCodes | uint8 | M | 参数。 |

## AddressingModeEnum8

该枚举定义 NetworkInterfacesTraitPresentValueStructure 中 `AddressingMode` 字段有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | DHCP | 通过 DHCP 动态分配 IP 地址 |
| 0x01 | Static | 静态配置 IP 地址 |
| 0x02 | Auto | 自动配置（如 IPv6 SLAAC 或链路本地地址） |
| 0xFF | Unspecified | 未指定或未知 |

## AddUserDefinedEffect Command Structure

AddUserDefinedEffect Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | EffectName | characterString | M | 新增场景名称，UTF-8 字节长度不得超过 MaxEffectNameLength。 |
| 1 | EffectData | characterString | M | 新增场景的原始数据，编码格式应与 EffectData 规范一致。 |

## AirflowDirectionEnum8

该枚举定义 AirflowDirection Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Forward | Forward |
| 0x01 | Reverse | Reverse |

## AirQualityEnum8

该枚举定义 AirQuality Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Unknown | 未知 |
| 0x01 | Excellent | 优 |
| 0x02 | Good | 良 |
| 0x03 | Fair | 中 |
| 0x04 | Inferior | 差 |
| 0x05 | Poor | 极差 |

## AlarmErrorEnum8

该枚举定义 AlarmError Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | SUCCESS | 成功 |
| 0x01 | FAILURE | 失败 |
| 0x02 | NOT_ALLOWED | 不允许 |

## AllpagesTraitPresentValueStructure

## AllpagesWidgetStructure

该结构体定义 Allpages 页面 Widgets 列表元素字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | WidgetID | uint16 | M | 控件 ID。 |
| 1 | WidgetPosition | [WidgetPositionStructure](./data-types#widgetpositionstructure) | M | 控件位置与尺寸。 |
| 2 | WidgetMode | enum8 | M | 控件模式：0=Basic，1=Lighting-ColorTempControl，2=Lighting-BrightnessControl，3=Lighting-ColorControl，4=AirCondition-TemperatureControl，5=AirCondition-FanSpeed，6=WindowCovering-Operation，7=DoorLock-Operation。 |
| 3 | AssociatedDeviceInfo | [AssociatedDeviceInfoStructure](./data-types#associateddeviceinfostructure) | M | 关联设备信息。 |

## AnomalySoundDetectedEnum8

该枚举定义 AnomalySoundDetected Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | CryDetected | 检测到哭声 |
| 0x02 | SnoreDetected | 检测到鼾声 |
| 0x03 | LoudSoundDetected | 检测到较大声响 |
| 0x04 | BabyCryDetected | 检测到婴儿啼哭 |
| 0x05 | AlarmSoundDetected | 检测到报警器报警音 |
| 0x06 | DogBarkDetected | 检测到宠物吠叫 |
| 0x07 | CoughDetected | 检测到咳嗽 |

## AntiDeletionLevelSettingEnum8

该枚举定义 AntiDeletionLevelSetting Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | EnableResetAndFactory | 启用重置网络和恢复出厂 |
| 0x01 | DisableFactoryOnly | 仅禁用恢复出厂 |
| 0x02 | DisableResetAndFactory | 禁用重置网络和恢复出厂 |

## AntiDeletionNotificationEnum8

该枚举定义 AntiDeletionNotification Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | Trigger | 触发 |

## AntiDeletionSettingEnum8

该枚举定义 AntiDeletionSetting Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Disable | 禁用 |
| 0x01 | Enable | 启用 |

## AssociatedDeviceInfoStructure

该结构体定义 Widget 的 AssociatedDeviceInfo 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | DeviceName | characterString | M | 关联设备名称。 |
| 1 | DeviceTypeID | uint16 | M | Aqara 设备类型 ID。 |
| 2 | DeviceIcon | characterString | M | 设备图标。 |
| 3 | DeviceID | characterString | M | 目标设备 Device ID。 |
| 4 | EndPointID | uint16 | M | 目标 Endpoint ID。 |
| 5 | ParentID | characterString | O | 父设备 Device ID；同一 Matter 控制器下可省略。 |
| 6 | ModelValue | characterString | O | 非 Matter 设备的模型值；场景无需设置。 |

## AttitudeDetectedEnum8

该枚举定义 AttitudeDetected Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Normal | 正常 |
| 0x01 | FallDown | 跌倒 |
| 0x02 | SuspectedFall | 疑似跌倒 |

## AuthenticationStatusEnum8

该枚举定义 AuthenticationStatus Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | NotAuthenticated | 未认证 |
| 0x01 | CertifiedAndValid | 已认证且合法 |
| 0x02 | NoCertificate | 没证书 |
| 0x03 | CertificateInvalid | 有证书但不合法 |

## BatChargeStateEnum8

该枚举定义 BatChargeState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Unknown | 未知 |
| 0x01 | TheBatteryIsCharging | 正在充电 |
| 0x02 | TheBatteryIsAtFullCharge | 已充满电 |
| 0x03 | TheBatteryIsNotCharging | 未在充电 |
| 0x04 | TheBatteryIsNotChargeable | 不可充电 |

## BatReplaceabilityEnum8

该枚举定义 BatReplaceability Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Unspecified | 未指定 |
| 0x01 | NotReplaceable | 不可更换 |
| 0x02 | UserReplaceable | 用户可更换 |
| 0x03 | FactoryReplaceable | 工厂可更换 |

## BindingTraitPresentValueStructure

该结构体定义 Binding Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | DeviceID | characterString | M | 目标设备的 Device ID。最大长度 32 字节。 |
| 1 | ParentID | characterString | O | 目标设备所属父设备 Device ID。若与绑定方处于同一首选 Matter 控制器，则无需设置。最大长度 32 字节。 |
| 2 | GroupID | uint32 | O | 绑定目标为设备组时 SHALL 设置（Conformance: GROUP）。 |
| 3 | ModelValue | characterString | O | 针对非 Matter 设备 SHALL 设置目标模型值（Conformance: !MATTER）。最大长度 32 字节。 |
| 4 | EndPointID | uint32 | M | 目标设备的 Endpoint ID。 |
| 5 | DeviceTypeID | uint32 | M | 目标设备的 Aqara Device Type ID。 |

## BuiltInEffectsTraitPresentValueStructure

该结构体定义 BuiltInEffects Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | EffectID | uint8 | M | 内置场景 ID。 |
| 1 | BuiltInEffectLabelID | enum8 | M | 内置场景标签 ID。 |

## ButtonEventEnum8

该枚举定义 ButtonEvent Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | SinglePress | 单击 |
| 0x01 | DoublePress | 双击 |
| 0x02 | LongPress | 长按 |
| 0x03 | LongPressRelease | 长按释放 |

## CCAModeEnum8

该枚举定义 CCAMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | CCA1 | CCA1模式 |
| 0x01 | CCA1 | CCA2模式 |

## ChangeIndicationEnum8

该枚举定义 ChangeIndication Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | OK | OK |
| 0x01 | Warming | 警告 |
| 0x02 | Critical | 紧急 |

## ChangeTemperature Command Structure

ChangeTemperature Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | SetTemperature | float | M | 设置温度；默认值为 `20`。 |

## ChangeTemperatureLevel Command Structure

ChangeTemperatureLevel Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | SetTemperatureLevel | uint8 | M | 设置温度等级；默认值为 `0`。 |

## ChangeToMode Command Structure

ChangeToMode Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | CurrentMode | uint16 | M | 当前模式。 |

## ChangeToModeResponseEnum16

该枚举定义 ChangeToModeResponse Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Success | 成功 |
| 0x01 | UnsupportedMode | 不支持的模式 |
| 0x02 | GenericFailure | 一般故障 |
| 0x03 | InvalidInMode | 模式下无效 |
| 0x41 | Stuck | 被卡住 |
| 0x42 | DustBinMissing | 尘盒缺失 |
| 0x43 | DustBinFull | 尘盒已满 |
| 0x44 | WaterTankEmpty | 水箱为空 |
| 0x45 | WaterTankMissing | 水箱缺失 |
| 0x46 | WaterTankLidOpen | 水箱盖未关闭 |
| 0x47 | MopCleaningPadMissing | 拖布清洁垫缺失 |
| 0x48 | BatteryLow | 电池电量低 |

## ChildDeviceProvisioningStateEnum8

该枚举定义 ChildDeviceProvisioningState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | NotProvisioning | 未处于配网状态 |
| 0x01 | Provisioning | 处于配网状态 |

## ClearWeekDaySchedule Command Structure

ClearWeekDaySchedule Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | WeekDayIndex | uint8 | M | 工作日索引；默认值为 `1`。 |
| 1 | UserIndex | uint16 | M | 用户索引；默认值为 `1`。 |

## ClearYearDaySchedule Command Structure

ClearYearDaySchedule Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | YearDayIndex | uint8 | M | 年日序号；默认值为 `1`。 |
| 1 | UserIndex | uint16 | M | 用户索引；默认值为 `1`。 |

## CO2LevelEnum8

该枚举定义 CO2Level Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Excellent | 优 |
| 0x01 | LightPollution | 轻度污染 |
| 0x02 | HeavyPollution | 重度污染 |

## ColorModeEnum8

该枚举定义 ColorMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | HSV | 由 Hue 与 Saturation 决定当前颜色 |
| 0x01 | XY | 由 CurrentX 与 CurrentY 决定当前颜色 |
| 0x02 | ColorTemperature | 由色温值决定当前颜色 |

## ColorPointRGBXYPresentValueStructure

该结构体定义 ColorPointRGBXY 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | ColorPointRX | uint16 | M | 该参数表示设备当前红色原色点在 CIE xyY 色彩空间中定义的归一化色度值 x。x 与 ColorPointRX 属性满足关系：x = ColorPointRX / 65536（ColorPointRX 取值范围为 0 至 65279，含边界）。 |
| 1 | ColorPointRY | uint16 | M | 该参数表示设备当前红色原色点在 CIE xyY 色彩空间中定义的归一化色度值 y。y 与 ColorPointRY 属性满足关系：y = ColorPointRY / 65536（ColorPointRY 取值范围为 0 至 65279，含边界）。 |
| 2 | ColorPointGX | uint16 | M | 该参数表示设备当前绿色原色点在 CIE xyY 色彩空间中定义的归一化色度值 x。x 与 ColorPointGX 属性满足关系：x = ColorPointGX / 65536（ColorPointGX 取值范围为 0 至 65279，含边界）。 |
| 3 | ColorPointGY | uint16 | M | 该参数表示设备当前绿色原色点在 CIE xyY 色彩空间中定义的归一化色度值 y。y 与 ColorPointGY 属性满足关系：y = ColorPointGY / 65536（ColorPointGY 取值范围为 0 至 65279，含边界）。 |
| 4 | ColorPointBX | uint16 | M | 该参数表示设备当前蓝色原色点在 CIE xyY 色彩空间中定义的归一化色度值 x。x 与 ColorPointBX 属性满足关系：x = ColorPointBX / 65536（ColorPointBX 取值范围为 0 至 65279，含边界）。 |
| 5 | ColorPointBY | uint16 | M | 该参数表示设备当前蓝色原色点在 CIE xyY 色彩空间中定义的归一化色度值 y。y 与 ColorPointBY 属性满足关系：y = ColorPointBY / 65536（ColorPointBY 取值范围为 0 至 65279，含边界）。 |

## ConfigStaticIPAddr Command Structure

ConfigStaticIPAddr Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | IPAddress | characterString | O | IP 地址，字符串（最大长度 32）。示例：`192.168.1.100`。 |
| 1 | SubnetMask | characterString | O | 子网掩码，字符串（最大长度 32）。示例：`255.255.255.0`。 |
| 2 | DefaultGateway | characterString | O | 默认网关，字符串（最大长度 32）。示例：`192.168.1.1`。 |
| 3 | PrimaryDNS | characterString | O | 首选 DNS，字符串（最大长度 32）。示例：`8.8.8.8`。 |
| 4 | SecondaryDNS | characterString | O | 备选 DNS，字符串（最大长度 32）。示例：`8.8.4.4`。 |
| 5 | InterfaceName | characterString | O | 接口名称，字符串（最大长度 32）。示例：`eth0`。为空时表示应用于所有网络接口。 |
| 6 | AddressingMode | enum8 | O | 地址分配模式。`0` 表示切换为 DHCP 获取 IP（此时 `IPAddress`、`SubnetMask`、`PrimaryDNS`、`DefaultGateway`、`SecondaryDNS` 参数会被忽略）；`1` 表示静态 IP 模式（可配置上述网络参数）。若该字段不存在，默认按静态 IP 模式处理。 |

## ConfigurationVersionTraitPresentValueStructure

该结构体定义 ConfigurationVersion Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | ConfigurationVersion | uint32 | M | 设备配置版本号。 |
| 1 | ThingModelMd5 | characterString | M | 物模型 MD5 校验值。 |

## ConnectToHomeStation Command Structure

ConnectToHomeStation Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | SSID | characterString | M | SSID。 |
| 1 | Credentials | characterString | M | 凭证。 |

## ConnectToHomeStationResponse Command Structure

ConnectToHomeStationResponse Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | ConnectToHomeStationResponse | enum8 | M | 连接家庭基站反馈。 |

## CubeEventEnum8

该枚举定义 CubeEvent Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Flip90 | 翻转90° |
| 0x01 | Flip180 | 翻转180° |
| 0x02 | Push | 轻推 |
| 0x03 | TapTwice | 拿起敲两下 |
| 0x04 | Shake | 摇一摇 |
| 0x05 | Swing | 甩一下 |
| 0x06 | Rotate | 旋转 |
| 0x07 | TriggeredAfter1Minute | 静止后触动 |
| 0x0A | PickUpHold | 拿起保持 |

## CubeWorkModeSettingEnum8

该枚举定义 CubeWorkModeSetting Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Action Mode | 动作模式 |
| 0x01 | Scene Mode | 场景模式 |

## CurOperationModeEnum8

该枚举定义 CurOperationMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | UnjoinedMode | 未入网模式 |
| 0x01 | NewlyJoinedMode | 新入网模式 |
| 0x02 | PollingMode | 仓储轮询模式 |
| 0x03 | UserMode | 用户模式 |
| 0x04 | DegradedMode | 亚健康模式 |
| 0x05 | RejoinMode | 仓储重连模式 |

## CurrentGarageDoorStateEnum8

该枚举定义 CurrentGarageDoorState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | Unknown | 未知 |
| 0x02 | Open | 打开 |
| 0x03 | Closed | 关闭 |
| 0x04 | Opening | 打开中 |
| 0x05 | Closing | 关闭中 |
| 0x06 | NotFullyOpen | 未完全打开 |
| 0x07 | NotFullyClosed | 未完全关闭 |

## CurrentPlaybackStateEnum8

该枚举定义 CurrentPlaybackState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Playing | 播放中 |
| 0x01 | Paused | 暂停 |
| 0x02 | Stopped | 停止 |
| 0x03 | Buffering | 缓冲 |
| 0x04 | FastForwarding | 快进 |
| 0x05 | Rewinding | 快退 |

## CurrentSafetyManagementEnum8

该枚举定义 CurrentSafetyManagement Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | CurrentNormal | 电流正常 |
| 0x01 | OverCurrentAnomaly | 过流异常 |
| 0x02 | OverCurrentProtection | 过流保护 |

## CurrentValveStateEnum8

该枚举定义 CurrentValveState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Closed | 关闭 |
| 0x01 | Open | 打开 |
| 0x02 | Transitioning | 过渡 |

## DetectionModeSettingEnum16

该枚举定义 DetectionModeSetting Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Unconfigured | 未配置 |
| 0x03 | DoorWindowState | 门窗状态检测 |
| 0x05 | ObjectOrientation | 物品姿态检测 |

## DoorLockAlarmTraitPresentValueStructure

该结构体定义 DoorLockAlarm Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | AlarmCode | enum8 | M | 门锁告警类型。 |

## DoorStateEnum8

该枚举定义 DoorState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | DoorOpen | 门未关 |
| 0x01 | DoorClosed | 门已关 |
| 0x02 | DoorJammed | 门卡住 |
| 0x03 | DoorForcedOpen | 门强制打开 |
| 0x04 | DoorUnspecifiedError | 门异常 |
| 0x05 | DoorAjar | 门虚掩 |

## EffectInfoTraitPresentValueStructure

该结构体定义 EffectInfo Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | EffectID | uint8 | M | 场景 ID。 |
| 1 | EffectData | characterString | M | 场景数据。 |
| 2 | EffectName | characterString | M | 场景名称。 |

## EndpointStructureNodeStructure

该结构体定义 EndpointStructure 嵌套子节点字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | EndpointId | uint16 | M | Endpoint ID。 |
| 1 | ChildEndpoints | list[[EndpointStructureNodeStructure](./data-types#endpointstructurenodestructure)] | O | 子 Endpoint 节点列表，用于表达嵌套层级。 |

## EndpointStructureTraitPresentValueStructure

该结构体定义 EndpointStructure Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | EndpointId | uint16 | M | 根 Endpoint ID。 |
| 1 | ChildEndpoints | list[[EndpointStructureNodeStructure](./data-types#endpointstructurenodestructure)] | O | 子 Endpoint 层级结构。 |

## EvseFaultStateEnum32

该枚举定义 EvseFaultState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | NoError | 无错误 |
| 0x01 | MeterFailure | 电表故障 |
| 0x02 | OverVoltage | 过电压 |
| 0x03 | UnderVoltage | 欠电压 |
| 0x04 | OverCurrent | 过电流 |
| 0x05 | ContactWetFailure | 接触湿故障 |
| 0x06 | ContactDryFailure | 接触干故障 |
| 0x07 | GroundFault | 地面故障 |
| 0x08 | PowerLoss | 电力丢失 |
| 0x09 | PowerQuality | 电力质量 |
| 0x0A | PilotShortCircuit | 引导短路 |
| 0x0B | EmergencyStop | 紧急停止 |
| 0x0C | EVDisconnected | 电动车断开 |
| 0x0D | WrongPowerSupply | 电源错误 |
| 0x0E | LiveNeutralSwap | 火线零线交换 |
| 0x0F | OverTemperature | 过温度 |
| 0xFF | Other | 其他 |

## EvseStateEnum32

该枚举定义 EvseState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | NotPluggedIn | 未插入 |
| 0x01 | PluggedInNoDemand | 插入但无需求 |
| 0x02 | PluggedInDemand | 插入有需求 |
| 0x03 | PluggedInCharging | 插入充电中 |
| 0x04 | PluggedInDischarging | 插入放电中 |
| 0x05 | SessionEnding | 会话结束 |
| 0x06 | Fault | 故障 |

## EvseSupplyStateEnum32

该枚举定义 EvseSupplyState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Disabled | 禁用 |
| 0x01 | ChargingEnabled | 允许充电 |
| 0x02 | DischargingEnabled | 允许放电 |
| 0x03 | DisabledError | 禁用错误 |
| 0x04 | DisabledDiagnostics | 禁用诊断 |
| 0x05 | EnabledChargingandDischarging | 允许充电和放电 |

## ExpressedStateEnum8

该枚举定义 ExpressedState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Normal | 正常 |
| 0x01 | SmokeAlarm | 烟雾泄漏 |
| 0x02 | COAlarm | CO泄漏 |
| 0x03 | BatteryAlert | 电池警告 |
| 0x04 | Testing | 测试中 |
| 0x05 | HardwareFault | 硬件错误 |
| 0x06 | EndOfService | 服务终止 |
| 0x07 | InterconnectSmoke | 互联烟雾报警 |
| 0x08 | InterconnectCO | 互联CO报警 |

## FanModeEnum8

该枚举定义 FanMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Off | 关闭 |
| 0x01 | LowSpeed | 低速 |
| 0x02 | MediumSpeed | 中速 |
| 0x03 | HighSpeed | 高速 |
| 0x04 | Auto | 自动 |
| 0x05 | Smart | 智能 |

## FeedEnum8

该枚举定义 Feed Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | RemoteFeeding | 喂养 |

## FlameDetectedEnum8

该枚举定义 FlameDetected Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | FlameDetected | 识别到火焰 |

## FreshAirModeEnum8

该枚举定义 FreshAirMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Manual | 手动 |
| 0x01 | Auto | 自动 |
| 0x02 | Exhaust | 排风 |
| 0x03 | HeatExchange | 热交换 |
| 0x04 | InternalCirculation | 内循环 |
| 0x05 | ExternalCirculation | 外循环 |
| 0x06 | PowerSaving | 省电 |
| 0x07 | Strong | 强劲 |
| 0x08 | FreshAir | 新风 |
| 0x09 | Bypass | 旁通 |

## GestureRecognitionReportEnum16

该枚举定义 GestureRecognitionReport Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x02 | Two | 二 |
| 0x04 | Four | 四 |
| 0x05 | Five | 五 |
| 0x06 | Eight | 八 |
| 0x0A | Good | 好 |
| 0x65 | TwoBothHands | 二（双手） |
| 0x66 | FourBothHands | 四（双手） |
| 0x67 | FiveBothHands | 五（双手） |
| 0x68 | EightBothHands | 八（双手） |

## GetEffectData Command Structure

GetEffectData Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | EffectID | uint8 | M | 待读取场景数据的场景 ID，应存在于 BuiltIn 或 UserDefined 列表中。 |

## GetWeekDaySchedule Command Structure

GetWeekDaySchedule Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | WeekDayIndex | uint8 | M | 工作日索引；默认值为 `1`。 |
| 1 | UserIndex | uint16 | M | 用户索引；默认值为 `1`。 |

## GetYearDaySchedule Command Structure

GetYearDaySchedule Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | YearDayIndex | uint8 | M | 年日序号；默认值为 `1`。 |
| 1 | UserIndex | uint16 | M | 用户索引；默认值为 `1`。 |

## GoToPositionPercentage Command Structure

GoToPositionPercentage Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | PositionPercentage | float | M | 开合比。 |
| 1 | TransitionTime | uint32 | O | 时间。 |

## GoToPositionRotationPercentage Command Structure

GoToPositionRotationPercentage Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | RotationPercentage | float | M | 倾斜旋转百分比。 |

## HeaterCoolerButtonEventEnum8

该枚举定义 HeaterCoolerButtonEvent Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | PowerShortPress | 电源短按 |
| 0x01 | FanSpeedShortPress | 风速短按 |
| 0x02 | TempUpShortPress | 温度+短按 |
| 0x03 | TempDownShortPress | 温度-短按 |
| 0x04 | ModeShortPress | 模式短按 |
| 0x10 | PowerMidPress | 电源中按 |
| 0x11 | FanSpeedMidPress | 风速中按 |
| 0x12 | TempUpMidPress | 温度+中按 |
| 0x13 | TempDownMidPress | 温度-中按 |
| 0x14 | ModeMidPress | 模式中按 |
| 0x30 | PowerLongPress | 电源长按 |
| 0x31 | FanSpeedLongPress | 风速长按 |
| 0x32 | TempUpLongPress | 温度+长按 |
| 0x33 | TempDownLongPress | 温度-长按 |
| 0x34 | ModeLongPress | 模式长按 |

## HeaterCoolerModeEnum8

该枚举定义 HeaterCoolerMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Cool | 制冷 |
| 0x01 | Heat | 制热 |
| 0x02 | Auto | 自动 |
| 0x03 | Fan | 送风 |
| 0x04 | Dry | 除湿 |
| 0x05 | Sleep | 睡眠 |
| 0x06 | FreshAir | 新风 |
| 0x07 | FloorHeat | 地暖 |
| 0x08 | HeatandFloorHeat | 强热 |
| 0x09 | FloorHeatandFreshAir | 地暖+新风 |
| 0x0A | EmergencyHeat | 速热 |
| 0x0B | Precooling | 预冷 |
| 0x0C | Off | 关闭 |
| 0x0D | Manual | 手动 |
| 0x0E | AntiFreeze | 防冻 |

## HeaterCoolerPolarityEnum8

该枚举定义 HeaterCoolerPolarity Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Forward | 正向（制热） |
| 0x01 | Reverse | 反向（制冷） |
| 0x02 | Bidirectional | 双向 |

## HomeKitPayloadTraitPresentValueStructure

该结构体定义 HomeKitPayload Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | QRCode | characterString | M | 配对二维码原始字符串数据。 |
| 1 | DigitalPairingCode | characterString | M | 8 位数字配对码。 |

## HomeStationCredentialsTraitPresentValueStructure

该结构体定义 HomeStationCredentials Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | SSID | characterString | M | 家庭基站 SSID。 |
| 1 | Credentials | characterString | M | 家庭基站连接凭证。 |

## HubConnectionStatusEnum8

该枚举定义 HubConnectionStatus Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Disconnected | 断开 |
| 0x01 | Connected | 连接 |

## HumanRecognitionReportEnum8

该枚举定义 HumanRecognitionReport Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | HumanDetected | 检测到人形事件 |

## Identify Command Structure

Identify Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | IdentifyTime | float | M | 设备继续自识别的剩余时间，单位为秒。值为 `0` 时，设备 MAY 使用默认识别时长。 |

## IdentifyTypeEnum8

该枚举定义 IdentifyType Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | None | None |
| 0x01 | Light | Light |
| 0x02 | LEDIndicator | LEDIndicator |
| 0x03 | AudibleBeep | AudibleBeep |
| 0x04 | Display | Display |
| 0x05 | Actuator | Actuator |

## InstallationOrientationConfigurationEnum16

该枚举定义 InstallationOrientationConfiguration Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Horizontal | 横向 |
| 0x01 | Vertical | 竖向 |

## InterfaceTypeEnum8

该枚举定义 NetworkInterfacesTraitPresentValueStructure 中 `Type` 字段有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Unspecified | 未指定 |
| 0x01 | WiFi | Wi-Fi 接口 |
| 0x02 | Ethernet | 以太网接口 |
| 0x03 | Cellular | 蜂窝网络接口 |
| 0x04 | Thread | Thread 接口 |

## IRKeyEnum32

该枚举定义 IRKey Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Reserved | Reserved |
| 0x01 | Power | Power |
| 0x02 | Mode | Mode |
| 0x03 | Temp+ | Temp+ |
| 0x04 | Temp- | Temp- |
| 0x05 | WindSpeed | WindSpeed |
| 0x25A5 | P_KEY | P_KEY |

## IRTypeEnum8

该枚举定义 IRType Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | AirConditioner | AirConditioner |
| 0x01 | TV | TV |
| 0x02 | Fan | Fan |
| 0x03 | TVBox | TVBox |
| 0x04 | Projector | Projector |
| 0x05 | SettopBox | SettopBox |
| 0x06 | DVD | DVD |
| 0x07 | Heater | Heater |
| 0x08 | AirPurifier | AirPurifier |
| 0x09 | Speaker | Speaker |
| 0x0A | Lighting | Lighting |
| 0x0B | Camera | Camera |
| 0x0C | Customize | Customize |

## LEDICProtocolEnum8

该枚举定义 LEDICProtocol Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | SID | 单线归零码 |

## LEDTypeEnum8

该枚举定义 LEDType Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | RGBW | RGBW |

## LightingDynamicSceneModeEnum8

该枚举定义 LightingDynamicSceneMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Breath | 呼吸 |
| 0x01 | ForwardRainbow | 正向彩虹 |
| 0x02 | WaterFlow | 流水 |
| 0x03 | Flicker | 闪烁 |
| 0x04 | Jump | 跳变 |
| 0x05 | ReverseRainbow | 反向彩虹 |
| 0x06 | Floating | 飘动 |
| 0x07 | Running | 跑动 |

## LightingModeSettingEnum8

该枚举定义 LightingModeSetting Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Level | 单色模式 |
| 0x01 | ColorTemp | 色温模式 |
| 0x02 | RGB | RGB模式 |
| 0x03 | RGBW模式 | RGBW模式 |

## LightingMusicSyncModeEnum8

该枚举定义 LightingMusicSyncMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Random | 随机 |
| 0x01 | Breath | 呼吸 |
| 0x02 | Rainbow | 彩虹 |
| 0x03 | WaterFlow | 流水 |

## LimitPointSettingEnum8

该枚举定义 LimitPointSetting Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | ClearLimitPoints | 清除限位点 |
| 0x01 | SetOpenLimit | 设置开限位点 |
| 0x02 | SetCloseLimit | 设置关限位点 |

## LoadSafetyManagementEnum8

该枚举定义 LoadSafetyManagement Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | LoadNormal | 负载正常 |
| 0x01 | OverLoadAnomaly | 过载异常 |
| 0x02 | OverLoadProtection | 过载保护 |

## LoadTypeEnum8

该枚举定义 LoadType Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | General | General |
| 0x01 | Linkable | Linkable |
| 0x02 | Pulsed | Pulsed |

## LockDoor Command Structure

LockDoor Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Code | characterString | O | 参数。 |

## LockOperationErrorTraitPresentValueStructure

该结构体定义 LockOperationError Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | LockOperationType | enum8 | M | 执行的门锁操作类型。 |
| 1 | OperationSource | enum8 | M | 门锁操作来源。 |
| 2 | OperationError | enum8 | M | 操作失败原因。 |
| 3 | UserIndex | uint16 | M | 执行门锁操作的用户索引。 |
| 4 | FabricID | uint64 | M | 执行门锁操作的 fabric ID。 |
| 5 | SourceNode | uint64 | M | 执行门锁操作的 node ID。 |
| 6 | Credentials | list[Struct] | O | 执行门锁操作时使用的凭证列表；无凭证时可为空。 |

## LockOperationTraitPresentValueStructure

该结构体定义 LockOperation Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | LockOperationType | enum8 | M | 执行的门锁操作类型。 |
| 1 | OperationSource | enum8 | M | 门锁操作来源。 |
| 2 | UserSource | enum8 | O | UserId 与 CredentialId 的来源。省略时默认 Lock Local。 |
| 3 | UserId | uint32 | O | 用户唯一标识。 |
| 4 | CredentialId | uint32 | O | 凭证唯一标识。 |
| 5 | MatterVendorId | uint16 | O | 当 OperationSource 为 Remote(Matter Ecosystem) 时表示 Matter Vendor ID。 |
| 6 | LockPosition | enum8 | O | 门锁操作位置。 |

## LockStateEnum8

该枚举定义 LockState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | NotFullyLocked | 锁舌异常 |
| 0x01 | Locked | 已上锁 |
| 0x02 | Unlocked | 未上锁 |

## LockUserChangeTraitPresentValueStructure

该结构体定义 LockUserChange Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | LockDataType | enum8 | M | 被变更的数据类型。 |
| 1 | DataOperationType | enum8 | M | 数据操作类型。 |
| 2 | OperationSource | enum8 | M | 执行操作的来源。 |
| 3 | UserIndex | uint16 | M | 执行门锁操作的用户索引。 |
| 4 | FabricID | uint64 | M | 执行门锁操作的 fabric ID。 |
| 5 | SourceNode | uint64 | M | 执行门锁操作的 node ID。 |
| 6 | DataIndex | uint16 | O | 被变更项目在 LockDataType 对应列表中的索引。 |

## MagicPairPayloadTraitPresentValueStructure

该结构体定义 MagicPairPayload Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | QRCode | characterString | M | 配对二维码原始字符串数据。 |
| 1 | DigitalPairingCode | characterString | M | 8 位数字配对码。 |

## ManualPullEventEnum32

该枚举定义 ManualPullEvent Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | ManualOpen | 手拉打开 |
| 0x02 | ManualClose | 手拉关闭 |
| 0x03 | ManualStop | 手拉停止 |

## MatterBindStateEnum8

该枚举定义 MatterBindState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Unbound | Unbound |
| 0x01 | Bound | Bound |

## MatterDACStateEnum8

该枚举定义 MatterDACState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | LoadingDAC | LoadingDAC |
| 0x01 | FailedDAC | FailedDAC |
| 0x02 | ReadyDAC | ReadyDAC |

## MatterPairingStateEnum8

该枚举定义 MatterPairingState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | NonMatterPairingState | 非 Matter 配网状态 |
| 0x01 | MatterPairingState | Matter 配网状态 |
| 0x02 | LimitExceeded | 超出限制 |
| 0x03 | InvalidDAC | 无效设备认证证书 |
| 0x04 | UnderPreparation | 准备中 |

## MatterPayloadTraitPresentValueStructure

该结构体定义 MatterPayload Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | QRCode | characterString | M | 配对二维码原始字符串数据。 |
| 1 | DigitalPairingCode | characterString | M | 11 或 21 位数字配对码。 |

## MediaPlaybackCommand Command Structure

MediaPlaybackCommand Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | MediaType | enum8 | M | 媒体类型；默认值为 `0`。 |
| 1 | Command | characterString | O | 命令内容。 |
| 2 | Title | characterString | O | 媒体名称。 |

## MotionDetectedEnum8

该枚举定义 MotionDetected Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Enter | 进入 |
| 0x01 | Left | 离开 |
| 0x02 | LeftIn | 左进 |
| 0x03 | RightOut | 右出 |
| 0x04 | RightIn | 右进 |
| 0x05 | LeftOut | 左出 |
| 0x06 | Access | 接近 |
| 0x07 | Away | 远离 |

## MotorOperationStatusEnum8

该枚举定义 MotorOperationStatus Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Closing | 关闭中 |
| 0x01 | Opening | 打开中 |
| 0x02 | Stopped | 停止 |
| 0x03 | ObstacleStop | 障碍物停止 |
| 0x04 | ObstacleLimitAutoRebound | 遇阻反弹 |

## MotorSpeedSettingEnum8

该枚举定义 MotorSpeedSetting Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | LowSpeed | 低速 |
| 0x01 | MediumSpeed | 中速 |
| 0x02 | HighSpeed | 高速 |

## MoveToColorTemperature Command Structure

MoveToColorTemperature Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | ColorTemperature | uint32 | M | 色温。 |
| 1 | Time | int32 | O | 时间。 |
| 2 | ExecuteIfOff | boolean | O | 关灯时是否执行命令。 |

## MoveToColorTemperatureBrightness Command Structure

MoveToColorTemperatureBrightness Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | ColorTemperature | uint32 | M | 色温。 |
| 1 | Level | float | M | 亮度；默认值为 `50`。 |
| 2 | Time | int32 | O | 时间。 |
| 3 | ExecuteIfOff | boolean | O | 关灯时是否执行命令。 |

## MoveToHueAndSaturation Command Structure

MoveToHueAndSaturation Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Hue | float | M | Hue。 |
| 1 | Saturation | float | M | 饱和度。 |
| 2 | Time | int32 | O | 时间。 |
| 3 | ExecuteIfOff | boolean | O | 关灯时是否执行命令。 |

## MoveToHueSaturationBrightness Command Structure

MoveToHueSaturationBrightness Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Hue | float | M | Hue。 |
| 1 | Saturation | float | M | 饱和度。 |
| 2 | Level | float | M | 亮度；默认值为 `50`。 |
| 3 | Time | int32 | O | 时间。 |
| 4 | ExecuteIfOff | boolean | O | 关灯时是否执行命令。 |

## MoveToXYColor Command Structure

MoveToXYColor Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | ColorX | uint16 | M | 颜色X值。 |
| 1 | ColorY | uint16 | M | 颜色Y值。 |
| 2 | Time | int32 | O | 时间。 |
| 3 | ExecuteIfOff | boolean | O | 关灯时是否执行命令。 |

## MoveToXYColorBrightness Command Structure

MoveToXYColorBrightness Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | ColorX | uint16 | M | 颜色X值。 |
| 1 | ColorY | uint16 | M | 颜色Y值。 |
| 2 | Level | float | M | 亮度；默认值为 `50`。 |
| 3 | Time | int32 | O | 时间。 |
| 4 | ExecuteIfOff | boolean | O | 关灯时是否执行命令。 |

## NetworkInterfacesTraitPresentValueStructure

该结构体定义 NetworkInterfaces Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Name | characterString | O | 网络接口的人类可读或系统级标识（如 `eth0`、`wlan0`），用于在设备操作系统内唯一标识该接口并用于配置或诊断引用。 |
| 1 | IsOperational | boolean | M | 指示该网络接口当前是否可正常收发数据。通常需同时满足链路可用、已分配 IP 地址、路由可达等条件。 |
| 2 | OffPremiseServicesReachableIPv4 | boolean | O | 指示设备是否可通过该接口使用 IPv4 访问外部（网外）服务（如云端地址）；`true` 表示具备 IPv4 外网连通性。 |
| 3 | OffPremiseServicesReachableIPv6 | boolean | O | 指示设备是否可通过该接口使用 IPv6 访问外部（网外）服务；`true` 表示具备 IPv6 外网连通性。 |
| 4 | HardwareAddress | characterString | M | MAC 地址字符串，最大长度 24。应为 48 位 IEEE MAC 或 64 位 IEEE MAC（EUI-64），采用大端/显示顺序（左侧为高位字节），示例：`00:1A:2B:3C:4D:5E`、`00:1A:2B:FF:FE:3C:4D:5E`。 |
| 5 | IPv4Addresses | list[characterString] | O | IPv4 地址列表，元素类型为字符串（最大长度 16），示例：`192.168.2.235`。 |
| 6 | IPv6Addresses | list[characterString] | O | IPv6 地址列表，元素类型为字符串（最大长度 40），示例：`2001:DB8:0:0:8:800:200C:417A`。 |
| 7 | Type | [InterfaceTypeEnum8](./data-types#interfacetypeenum8) | M | 接口类型。 |
| 8 | RSSI | int8 | O | 接收信号强度指示值（dBm），通常为负值（如 `-30`）。 |
| 9 | AddressingMode | [AddressingModeEnum8](./data-types#addressingmodeenum8) | O | 指示该接口当前 IP 地址配置模式。 |
| 10 | SSID | characterString | O | 当前连接 Wi-Fi 的网络名称（Service Set Identifier）。示例：`Home_2.4G`。 |
| 11 | BSSID | characterString | O | 当前连接接入点（AP）的 BSSID（MAC 地址）。示例：`34:AB:37:12:CD:EF`。 |
| 12 | RouterMAC | characterString | O | 当前上联网关/路由器的 MAC 地址。示例：`9C:3D:CF:AA:BB:CC`。 |
| 13 | Channel | uint8 | O | 当前 Wi-Fi 工作信道（Chanel/Channel）。示例：`1`、`6`、`11`、`149`。 |
| 14 | SubnetMask | characterString | O | 子网掩码/前缀信息，字符串（最大长度 32）。IPv4 场景填子网掩码（示例：`255.255.255.0`）；IPv6 场景建议填前缀长度表示（示例：`64`），未知或不适用可留空。 |
| 15 | DefaultGateway | characterString | O | 默认网关地址，字符串（最大长度 32）。可填 IPv4 或 IPv6 网关地址，示例：`192.168.1.1`、`fe80::1`。 |
| 16 | PrimaryDNS | characterString | O | 首选 DNS，字符串（最大长度 32）。可填 IPv4 或 IPv6 DNS 地址，示例：`8.8.8.8`、`2001:4860:4860::8888`。 |
| 17 | SecondaryDNS | characterString | O | 备选 DNS，字符串（最大长度 32）。可填 IPv4 或 IPv6 DNS 地址，示例：`8.8.4.4`、`2001:4860:4860::8844`。 |

## NightLightModeEnum8

该枚举定义 NightLightMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Off | 常关 |
| 0x01 | On | 常开 |
| 0x02 | Auto | 自动感应 |

## ObjectOrientationStateEnum8

该枚举定义 ObjectOrientationState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | HorizontalUp | 水平朝上 |
| 0x02 | HorizontalDown | 水平朝下 |
| 0x03 | Vertical | 垂直 |
| 0x04 | Tilted | 倾斜 |

## ObstacleLimitSensitivityEnum8

该枚举定义 ObstacleLimitSensitivity Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Low | 低 |
| 0x01 | MediumLow | 较低 |
| 0x02 | Medium | 中等 |
| 0x03 | MediumHigh | 较高 |
| 0x04 | High | 高 |

## OccupancySensorTypeEnum8

该枚举定义 OccupancySensorType Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | PIR | 被动红外 |
| 0x01 | Ultrasonic | 超声波 |
| 0x02 | PIRAndUltrasonic | 红外和超声波 |
| 0x03 | PhysicalContact | 物理接触 |
| 0x04 | Radar | 雷达 |
| 0x05 | Vision | 视觉 |

## OperationalErrorEnum16

该枚举定义 OperationalError Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | NoError | 无错误 |
| 0x01 | UnableToStartOrResume | 无法开始或恢复 |
| 0x02 | UnableToCompleteOperation | 无法完成操作 |
| 0x03 | CommandInvalidInState | 状态下命令无效 |

## OperationalStateEnum16

该枚举定义 OperationalState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Stopped | 停止 |
| 0x01 | Running | 运行中 |
| 0x02 | Paused | 暂停 |
| 0x03 | Error | 错误 |

## OutputNodeModeEnum32

该枚举定义 OutputNodeMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | WetMode | 湿节点模式 |
| 0x01 | DryPulseNormallyClosed | 干节点脉冲常闭模式 |
| 0x02 | DryPulseNormallyOpen | 干节点脉冲常开模式 |
| 0x03 | DryToggleMode | 干节点通断模式 |

## OutputSwitchingTypeEnum8

该枚举定义 OutputSwitchingType Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | NonResettableSwitch | 不可回弹开关 |
| 0x02 | MomentaryPushSwitch | 可回弹开关 |
| 0x03 | ActiveLowCloseRelay | 低电平闭合继电器 |
| 0x04 | InsertCardOnMode | 插卡通电模式 |
| 0x05 | InsertCardOffMode | 插卡断电模式 |

## OverCurrentResponseSettingEnum8

该枚举定义 OverCurrentResponseSetting Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | ShutdownSingle | 关断单个 |
| 0x01 | ShutdownAll | 关断全部 |

## PackageRecognitionReportEnum8

该枚举定义 PackageRecognitionReport Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | PackageAppeared | 包裹出现 |
| 0x02 | PackageDisappeared | 包裹消失 |

## PanTiltDirectionControl Command Structure

PanTiltDirectionControl Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Direction | uint8 | M | 方向。 |

## PetRecognitionReportEnum16

该枚举定义 PetRecognitionReport Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | CatDetected | 检测到猫 |
| 0x02 | DogDetected | 检测到狗 |
| 0x03 | CatOrDogDetected | 检测到猫狗 |

## PlaybackModeEnum8

该枚举定义 PlaybackMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Shuffle | Shuffle |
| 0x01 | Order | Order |
| 0x02 | SingleCycle | SingleCycle |
| 0x03 | AllCycle | AllCycle |

## PM2.5LevelEnum8

该枚举定义 PM2.5Level Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Unknown | Unknown |
| 0x01 | Excellent | 优 |
| 0x02 | Good | 良好 |
| 0x03 | Fair | 一般 |
| 0x04 | Inferior | 差 |
| 0x05 | Poor | 极差 |

## PowerModeEnum8

该枚举定义 PowerMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Unknown | 未知 |
| 0x01 | Mains | 市电 |
| 0x02 | Battery | 电池 |
| 0x03 | DCSource | 直流电源 |
| 0x04 | ACSource | 交流电源 |

## PreOperationModeEnum8

该枚举定义 PreOperationMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | UnjoinedMode | 未入网模式 |
| 0x01 | NewlyJoinedMode | 新入网模式 |
| 0x02 | PollingMode | 仓储轮询模式 |
| 0x03 | UserMode | 用户模式 |
| 0x04 | DegradedMode | 亚健康模式 |
| 0x05 | RejoinMode | 仓储重连模式 |

## PresenceConfigurationTraitPresentValueStructure

该结构体定义 PresenceConfiguration Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | HubList | list[characterString] | M | 网关的 Device ID 列表。 |
| 1 | Events | Struct | O | 事件配置，包含 DeviceID 和 ResList。 |
| 2 | States | Struct | O | 状态配置，包含 DeviceID 和 ResList。 |
| 3 | Entries | characterString | O | 入口配置。 |
| 4 | Config | uint32 | O | 配置项，例如 DetDuration、UnKnownDuration。 |

## PresenceEnum8

该枚举定义 Presence Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Unknown | 未知 |
| 0x01 | NoHumanDetected | 未检测到人 |
| 0x02 | HumanDetected | 检测到人 |

## PresetTraitPresentValueStructure

该结构体定义 Preset Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | PresetHandle | characterString | M | 预设句柄。 |
| 1 | PresetScenario | enum8 | M | 预设场景：1=Occupied，2=Unoccupied，3=Sleep，4=Wake，5=Vacation，6=GoingToSleep；7–254=Reserved。 |
| 2 | Name | characterString | O | 预设名称；若为 null 则省略该字段。 |
| 3 | CoolingSetpoint | float | O | 制冷设定温度，默认 26。 |
| 4 | HeatingSetpoint | float | O | 制热设定温度，默认 20。 |
| 5 | BuiltIn | boolean | M | 是否为内置预设，默认 false。 |

## PreviewEffect Command Structure

PreviewEffect Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | EffectData | characterString | M | 待预览场景的原始数据。编码格式应与新增/更新用户自定义场景一致。 |

## PreviewTransition Command Structure

PreviewTransition Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | OnTransitionTime | uint16 | M | 开灯过渡时间，单位为 100 毫秒。示例：10 表示 1 秒。 |
| 1 | OffTransitionTime | uint16 | M | 关灯过渡时间，单位为 100 毫秒。示例：10 表示 1 秒。 |
| 2 | DimmingCurvature | float | M | 预览使用的调光曲线幂值。值越大，低亮区停留越久。 |
| 3 | InitialBrightness | uint8 | M | 预览使用的初始亮度百分比，取值范围与 InitialBrightness Trait 约束一致。 |

## Reboot Command Structure

Reboot Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | RebootType | enum8 | M | 重启方式。 |
| 1 | Delay | uint32 | O | 延迟时长；默认值为 `0`。 |

## RebootReasonEnum8

该枚举定义 RebootReason Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Unspecified | 未知 |
| 0x01 | PowerOnReboot | 上电重启 |
| 0x02 | BrownOutReset | 低压复位 |
| 0x03 | SoftwareWatchdogReset | 软件看门狗复位 |
| 0x04 | HardwareWatchdogReset | 硬件看门狗复位 |
| 0x05 | SoftwareUpdateCompleted | 软件更新完成 |
| 0x06 | SoftwareReset | 软件重启 |

## RemoveUserDefinedEffect Command Structure

RemoveUserDefinedEffect Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | EffectID | uint8 | M | 待删除场景 ID。应为已存在且可删除的 UserDefined 场景。 |

## ResetModeEnum8

该枚举定义 ResetMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | FactoryReset | 恢复出厂 |
| 0x02 | NetworkReset | 只重置网络 |
| 0x03 | ConfigurationsReset | 只重置配置 |
| 0x04 | PSWReset | 只重置设备密码 |

## RingtonePlayIndexEnum32

该枚举定义 RingtonePlayIndex Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | PoliceCar1 | 警车音1 |
| 0x01 | PoliceCar2 | 警车音2 |
| 0x02 | SafetyAccident | 安全事故音 |
| 0x03 | MissileCountdown | 导弹倒计时 |
| 0x04 | GhostSound | 鬼叫声 |
| 0x05 | SniperRifle | 狙击枪 |
| 0x06 | IntenseBattle | 激战声 |
| 0x07 | AirRaidSiren | 空袭警报 |
| 0x08 | DogBark | 狗叫声 |
| 0x0A | Doorbell | 门铃音 |
| 0x0B | Knock | 敲门音 |
| 0x0C | Funny | 搞笑音 |
| 0x0D | AlarmClock | 闹钟音 |
| 0x0E | Arming | 布防声 |
| 0x0F | Disarming | 撤防声 |
| 0x14 | MiMix | MiMix |
| 0x15 | Enthusiastic | Enthusiastic |
| 0x16 | GuitarClassic | GuitarClassic |
| 0x17 | IceWorldPiano | IceWorldPiano |
| 0x18 | LeisureTime | LeisureTime |
| 0x19 | Childhood | Childhood |
| 0x1A | MorningStreamIet | MorningStreamIet |
| 0x1B | MusixBox | MusixBox |
| 0x1C | Orange | Orange |
| 0x2710 | DefaultRingtone | 默认铃声 |

## RingtonePlayStatusEnum8

该枚举定义 RingtonePlayStatus Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Stopped | 停止 |
| 0x01 | Playing | 播放 |

## RockSettingEnum8

该枚举定义 RockSetting Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Off | 关闭 |
| 0x01 | RockLeftRight | 左右 |
| 0x02 | RockUpDown | 上下 |
| 0x03 | RockRound | 循环 |
| 0x04 | Swing | 扫风 |
| 0x05 | FixedDirection | 固定 |
| 0x15 | Fixed | 固定风向 |

## RotationControl Command Structure

RotationControl Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Direction | enum8 | M | 方向。 |

## RotationDirectionEnum8

该枚举定义 RotationDirection Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Clockwise | Clockwise |
| 0x01 | CounterClockwise | CounterClockwise |

## RotationEventEnum8

该枚举定义 RotationEvent Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Rotate | 旋转 |
| 0x01 | HoldToRotate | 按住旋转 |
| 0x02 | InvertedRotate | 反面旋转 |

## SelectedMediaOutputSourceEnum8

该枚举定义 SelectedMediaOutputSource Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Internal | 内置 |
| 0x01 | Bluetooth | 蓝牙 |
| 0x02 | Aux | 辅助 |
| 0x03 | SPDIF | SPDIF |
| 0x04 | Coaxial | 同轴 |
| 0x05 | Audio1 | 音频1 |
| 0x06 | Audio2 | 音频2 |
| 0x07 | Audio3 | 音频3 |
| 0x08 | AV1 | AV1 |
| 0x09 | AV2 | AV2 |
| 0x0A | AV3 | AV3 |
| 0x0B | HDMI1 | HDMI1 |
| 0x0C | HDMI2 | HDMI2 |
| 0x0D | HDMI3 | HDMI3 |
| 0x0E | Other | 其他 |

## SelectedSourceEnum8

该枚举定义 SelectedSource Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Internal | Internal |
| 0x01 | Bluetooth | Bluetooth |
| 0x02 | External | External |

## SelectInput Command Structure

SelectInput Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | MediaInput | uint8 | M | 输入源；默认值为 `0`。 |

## SendIRCode Command Structure

SendIRCode Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Mode | enum8 | M | 模式。 |
| 1 | BrandId | uint32 | M | 品牌DI。 |
| 2 | RemoteId | uint32 | M | 遥控器ID。 |
| 3 | IRCode | characterString | O | 红外压缩码。 |
| 4 | IRCodeLength | uint16 | O | 红外原始数据长度。 |
| 5 | ACKey | characterString | O | 空调状态数据。 |
| 6 | Frequency | uint32 | M | 红外发射频率。 |

## SetActivePreset Command Structure

SetActivePreset Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | PresetHandle | characterString | M | 预设操作。 |

## SetBrightness Command Structure

SetBrightness Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Level | float | M | 目标亮度百分比。 |
| 1 | Time | int32 | O | 过渡时长；为空时由设备按默认策略执行。 |
| 2 | ExecuteIfOff | boolean | O | 关灯状态下是否仍执行本命令（不改变开关联动语义）。 |

## SetBrightnessWithOnOff Command Structure

SetBrightnessWithOnOff Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Level | float | M | 目标亮度百分比。 |
| 1 | Time | int32 | O | 过渡时长；为空时由设备按默认策略执行。 |

## SetWeekDaySchedule Command Structure

SetWeekDaySchedule Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | WeekDayIndex | uint8 | M | 工作日索引；默认值为 `1`。 |
| 1 | UserIndex | uint16 | M | 用户索引；默认值为 `1`。 |
| 2 | DaysMask | enum8 | M | 日期掩码。 |
| 3 | StartHour | uint8 | M | 开始小时；默认值为 `0`。 |
| 4 | StartMinute | uint8 | M | 开始分钟；默认值为 `0`。 |
| 5 | EndHour | uint8 | M | 结束小时；默认值为 `0`。 |
| 6 | EndMinute | uint8 | M | 结束分钟；默认值为 `0`。 |

## SetYearDaySchedule Command Structure

SetYearDaySchedule Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | YearDayIndex | uint8 | M | 年日序号；默认值为 `1`。 |
| 1 | UserIndex | uint16 | M | 用户索引；默认值为 `1`。 |
| 2 | LocalStartTime | uint64 | M | 本地开始时间。 |
| 3 | LocalEndTime | uint64 | M | 本地结束时间。 |

## SleepStageEnum8

该枚举定义 SleepStage Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | LightSleep | LightSleep |
| 0x01 | DeepSleep | DeepSleep |
| 0x02 | REM | REM |

## SleepStateEnum8

该枚举定义 SleepState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Unknown | 未知 |
| 0x01 | Awake | 清醒 |
| 0x02 | Sleep | 入睡 |

## SmokeChamberContaminationEnum8

该枚举定义 SmokeChamberContamination Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Normal | 监测正常 |
| 0x01 | Contaminated | 烟室污染 |

## SmokeStateEnum8

该枚举定义 SmokeState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Normal | 正常 |
| 0x01 | Warning | 警告 |
| 0x02 | Critical | 严重 |

## StartEffect Command Structure

StartEffect Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | EffectID | enum8 | M | 待启动场景 ID，应为设备当前支持的 BuiltIn 或 UserDefined 场景。 |

## StartUpOnOffEnum8

该枚举定义 StartUpOnOff Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Off | 上电关闭 |
| 0x01 | On | 上电开启 |
| 0x02 | Toggle | 上电反转 |
| 0x03 | Hold | 上电记忆 |

## StartUpXYColorTraitPresentValueStructure

## StateOfLowBatEnum8

该枚举定义 StateOfLowBat Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | False | 电量充足 |
| 0x01 | True | 低电量 |
| 0x02 | Critical | 电量耗尽 |

## StationaryStateEnum8

该枚举定义 StationaryState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | InMotion | 运动中 |
| 0x01 | Stationary | 静止 |

## StepBrightness Command Structure

StepBrightness Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Size | float | M | 亮度步进增量，正值上调、负值下调。 |
| 1 | Time | int32 | O | 过渡时长；为空时由设备按默认策略执行。 |
| 2 | ExecuteIfOff | boolean | O | 关灯状态下是否仍执行本命令（不改变开关联动语义）。 |

## StepBrightnessWithOnOff Command Structure

StepBrightnessWithOnOff Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Size | float | M | 亮度步进增量，正值上调、负值下调。 |
| 1 | Time | int32 | O | 过渡时长；为空时由设备按默认策略执行。 |

## StepColorTemperature Command Structure

StepColorTemperature Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Size | int32 | M | 增量值。 |
| 1 | Time | int32 | O | 时间。 |
| 2 | ExecuteIfOff | boolean | O | 关灯时是否执行命令。 |

## StepPosition Command Structure

StepPosition Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Size | float | M | 增量值。 |
| 1 | Time | int32 | O | 时间。 |

## SweeperStateEnum8

该枚举定义 SweeperState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Cleaning | 清扫中 |
| 0x01 | Paused | 暂停 |
| 0x02 | NotCleaning | 未清扫 |

## TargetGarageDoorStateEnum8

该枚举定义 TargetGarageDoorState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Closed | 关闭 |
| 0x01 | Open | 打开 |

## TargetPlaybackStateEnum8

该枚举定义 TargetPlaybackState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Play | 播放 |
| 0x01 | Pause | 暂停 |
| 0x02 | Stop | 停止 |
| 0x03 | StartOver | 重播 |
| 0x04 | FastForward | 快进 |
| 0x05 | Rewind | 快退 |
| 0x06 | Previous | 上一首 |
| 0x07 | Next | 下一首 |
| 0x08 | PlaySpecificList | 播放指定歌单 |

## TemperatureSafetyManagementEnum8

该枚举定义 TemperatureSafetyManagement Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | TempNormal | 温度正常 |
| 0x01 | OverTempAnomaly | 过温异常 |
| 0x02 | OverTempProtection | 过温保护 |
| 0x0B | UnderTempAnomaly | 低温异常 |

## TemperatureUIDisplayModeEnum8

该枚举定义 TemperatureUIDisplayMode Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Celsius | 摄氏度 |
| 0x01 | Fahrenheit | 华氏度 |

## ThreadDiagnosticsTraitPresentValueStructure

该结构体定义 ThreadDiagnostics Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Channel | uint8 | M | The Thread network radio channel。 |
| 1 | ExtendedPANID | uint64 | M | The Thread network extended PAN identifier。 |
| 2 | NetworkKey | octetString | M | The Thread network key。 |
| 3 | NetworkName | characterString | M | The Thread network name。 |
| 4 | PANID | uint16 | M | The Thread network PAN identifier。 |
| 5 | PSKc | octetString | M | The Thread network pre-shared key for the Commissioner。 |
| 6 | ActiveTimeStamp | uint64 | M | Active timestamp。 |
| 7 | SecurityPolicy | characterString | M | Security policy。 |
| 8 | OperationalDataset | octetString | M | Thread Network Parameters dataset。 |
| 9 | RSSI | int8 | O | RSSI for the Thread network client in dBm。 |
| 10 | LQI | uint8 | O | Link quality indicator。 |
| 11 | BorderAgentID | octetString | O | Identifier of an active Thread network Border Agent。 |
| 12 | MeshLocalPrefix | characterString | O | Prefix of all interfaces reachable within the same Thread network。 |
| 13 | CommissionerCredential | characterString | O | UTF-8 Commissioner Credential, 6 to 255 characters。 |
| 14 | Version | characterString | O | OpenThread Version。 |
| 15 | Role | uint8 | O | Thread Role bit mask。 |

## TopFaceChangedEnum8

该枚举定义 TopFaceChanged Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Face 1 Up | 第1面朝上 |
| 0x01 | Face 2 Up | 第2面朝上 |
| 0x02 | Face 3 Up | 第3面朝上 |
| 0x03 | Face 4 Up | 第4面朝上 |
| 0x04 | Face 5 Up | 第5面朝上 |
| 0x05 | Face 6 Up | 第6面朝上 |

## TopFaceEnum8

该枚举定义 TopFace Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Face 1 Up | 第1面朝上 |
| 0x01 | Face 2 Up | 第2面朝上 |
| 0x02 | Face 3 Up | 第3面朝上 |
| 0x03 | Face 4 Up | 第4面朝上 |
| 0x04 | Face 5 Up | 第5面朝上 |
| 0x05 | Face 6 Up | 第6面朝上 |

## TriggerWarning Command Structure

TriggerWarning Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Warning | enum8 | M | 警告。 |

## TriggerZigbeeNetworkInfoUpload Command Structure

TriggerZigbeeNetworkInfoUpload Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | InstallCode | characterString | M | 安装代码。 |
| 1 | MAC | characterString | M | MAC地址。 |

## UnboltDoor Command Structure

UnboltDoor Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Code | characterString | O | 编码。 |

## UnlockDoor Command Structure

UnlockDoor Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Code | characterString | O | 解锁密钥。 |

## UpdateUserDefinedEffect Command Structure

UpdateUserDefinedEffect Command 的参数结构体。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | EffectID | uint8 | M | 待更新场景 ID。应为已存在 UserDefined 场景。 |
| 1 | EffectData | characterString | M | 更新后的场景数据，编码格式应与 EffectData 规范一致。 |

## UserDefinedEffectsTraitPresentValueStructure

该结构体定义 UserDefinedEffects Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | EffectID | uint8 | M | 用户自定义场景 ID。 |
| 1 | EffectName | characterString | M | 场景名称。 |

## VacuumOperationalErrorEnum16

该枚举定义 VacuumOperationalError Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | NoError | 无错误 |
| 0x01 | UnableToStartOrResume | 无法开始或恢复 |
| 0x02 | UnableToCompleteOperation | 无法完成操作 |
| 0x03 | CommandInvalidInState | 状态下命令无效 |
| 0x40 | FailedToFindChargingDock | 未能找到充电座 |
| 0x41 | Stuck | 被卡住 |
| 0x42 | DustBinMissing | 尘盒缺失 |
| 0x43 | DustBinFull | 尘盒已满 |
| 0x44 | WaterTankEmpty | 水箱为空 |
| 0x45 | WaterTankMissing | 水箱缺失 |
| 0x46 | WaterTankLidOpen | 水箱盖未关闭 |
| 0x47 | MopCleaningPadMissing | 拖布清洁垫缺失 |

## VacuumOperationalStateEnum16

该枚举定义 VacuumOperationalState Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Stopped | 停止 |
| 0x01 | Running | 运行中 |
| 0x02 | Paused | 暂停 |
| 0x03 | Error | 错误 |
| 0x40 | SeekingCharger | 寻找充电器 |
| 0x41 | Charging | 充电中 |
| 0x42 | Docked | 停靠 |

## VehicleDetectedEnum8

该枚举定义 VehicleDetected Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | VehicleDetected | 识别到车辆 |

## VibrationEventEnum8

该枚举定义 VibrationEvent Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | ThreeKnocks | 三击 |
| 0x01 | Move | 移动 |
| 0x02 | Vibrate | 震动 |
| 0x03 | Tilt | 倾斜 |
| 0x04 | FreeFall | 跌落 |

## VOCQualityEnum8

该枚举定义 VOCQuality Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Unknown | 未知 |
| 0x01 | Excellent | 优 |
| 0x02 | Good | 良好 |
| 0x03 | Fair | 一般 |
| 0x04 | Inferior | 差 |
| 0x05 | Poor | 极差 |

## VoiceLanguageEnum8

该枚举定义 VoiceLanguage Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x01 | Chinese | 中文 |
| 0x02 | English | 英文 |
| 0x03 | Cantonese | 粤语 |
| 0x04 | Spanish | 西班牙语 |
| 0x05 | Russian | 俄语 |
| 0x06 | Korean | 韩语 |
| 0x07 | PortugueseBR | 巴西语 |
| 0x08 | German | 德语 |
| 0x09 | French | 法语 |
| 0x0A | Italian | 意大利语 |
| 0x0B | Japanese | 日语 |
| 0x0C | Polish | 波兰语 |
| 0x0D | Czech | 捷克语 |
| 0x0E | Slovak | 斯洛伐克语 |
| 0x0F | Turkish | 土耳其语 |
| 0x10 | Hungarian | 匈牙利语 |
| 0x11 | Dutch | 荷兰语 |
| 0x12 | Greek | 希腊语 |
| 0x13 | Arabic | 阿拉伯语 |

## VoltageSafetyManagementEnum8

该枚举定义 VoltageSafetyManagement Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | VoltageNormal | 电压正常 |
| 0x01 | OverVoltageAnomaly | 过压异常 |
| 0x02 | OverVoltageProtection | 过压保护 |
| 0x03 | UnderVoltageProtection | 欠压保护 |

## WhitePointXYPresentValueStructure

该结构体定义 WhitePointXY 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | WhitePointX | uint16 | M | 该参数表示设备当前白点在 CIE xyY 色彩空间中定义的归一化色度值 x。x 与 WhitePointX 属性满足关系：x = WhitePointX / 65536（WhitePointX 取值范围为 0 至 65279，含边界）。 |
| 1 | WhitePointY | uint16 | M | 该参数表示设备当前白点在 CIE xyY 色彩空间中定义的归一化色度值 y。y 与 WhitePointY 属性满足关系：y = WhitePointY / 65536（WhitePointY 取值范围为 0 至 65279，含边界）。 |

## WidgetPositionStructure

该结构体定义 Widget 的 WidgetPosition 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | x | uint16 | M | 控件 X 坐标。 |
| 1 | y | uint16 | M | 控件 Y 坐标。 |
| 2 | width | uint16 | M | 控件宽度。 |
| 3 | height | uint16 | M | 控件高度。 |

## WiFiDiagnosticsTraitPresentValueStructure

该结构体定义 WiFiDiagnostics Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | BSSID | characterString | M | The Wi-Fi BSSID。 |
| 1 | Encryption | uint8 | O | 0: No Encryption；1: WEP；2: WPA；3: WPA/WPA2；4: WPA2；5: WPA3。 |
| 2 | Version | uint8 | O | The Wi-Fi Version：0:a，1:b，2:g，3:n，4:ac，5:ax。 |
| 3 | Channel | uint8 | M | The Wi-Fi network Channel。 |
| 4 | RSSI | int8 | M | RSSI for the WiFi network client in dBm。 |
| 5 | NetworkName | characterString | M | The Wi-Fi network name。 |
| 6 | IPV4Address | characterString | O | IPV4 Address。 |
| 7 | IPV6Address | characterString | O | IPV6 Address。 |
| 8 | SubnetMask | characterString | O | Subnet mask。 |
| 9 | Gateway | characterString | O | Gateway address。 |
| 10 | DNS | characterString | O | DNS server。 |
| 11 | AlternateDNS | characterString | O | Backup DNS server。 |

## WindowCoveringCategoryEnum8

该枚举定义 WindowCoveringCategory Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | RollerShade | 卷帘 |
| 0x01 | RollerShade2Motor | 双层卷帘 |
| 0x02 | RollerShadeExterior | 室外卷帘 |
| 0x03 | RollerShadeExterior2Motor | 双层室外卷帘 |
| 0x04 | Drapery | 窗帘 |
| 0x05 | Awning | 遮阳篷 |
| 0x06 | Shutter | 百叶帘 |
| 0x07 | TiltBlindTiltOnly | 仅倾斜百叶帘 |
| 0x08 | TiltBlindLiftAndTilt | 倾斜和升降百叶帘 |
| 0x09 | ProjectorScreen | 投影屏幕 |
| 0xFF | Unknown | 未知 |

## WindowCoveringOpenOrientationEnum8

该枚举定义 WindowCoveringOpenOrientation Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | LeftRight | 左右双开 |
| 0x01 | LeftToRight | 从左向右 |
| 0x02 | RightToLeft | 从右向左 |
| 0x03 | Up | 从下向上 |
| 0x04 | Horizontal | 左右开合 |
| 0x05 | Vertical | 上下开合 |

## WindowCoveringTypeEnum8

该枚举定义 WindowCoveringType Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | RollerShade | 卷帘 |
| 0x01 | VenetianBlind | 百叶帘 |
| 0x02 | ShangrilaBlind | 香格里拉百叶帘 |
| 0x03 | ZebraBlind | 斑马帘 |
| 0x04 | Curtain | 开合帘 |
| 0x05 | VerticalVenetianBlind | 梦幻帘 |
| 0x1F | RomanShade | 罗马帘 |
| 0x20 | BalloonShade | 气球帘 |
| 0x21 | WovenWood | 编织木帘 |
| 0x22 | PleatedShade | 褶皱帘 |
| 0x23 | CellularShade | 蜂巢帘 |
| 0x24 | LayeredShade | 分层帘 |
| 0x25 | LayeredShade2D | 2D分层帘 |
| 0x26 | SheerShade | 透光帘 |
| 0x27 | TiltOnlyInteriorBlind | 仅倾斜室内百叶帘 |
| 0x28 | InteriorBlind | 室内百叶帘 |
| 0x29 | VerticalBlindStripCurtain | 垂直百叶帘条形窗帘 |
| 0x2A | InteriorVenetianBlind | 室内威尼斯百叶帘 |
| 0x2B | ExteriorVenetianBlind | 室外威尼斯百叶帘 |
| 0x2C | LateralLeftCurtain | 左侧滑动窗帘 |
| 0x2D | LateralRightCurtain | 右侧滑动窗帘 |
| 0x2E | CentralCurtain | 中央窗帘 |
| 0x2F | RollerShutter | 卷帘门 |
| 0x30 | ExteriorVerticalScreen | 室外垂直遮阳帘 |
| 0x31 | AwningTerracePatio | 露台遮阳篷 |
| 0x32 | AwningVerticalScreen | 垂直遮阳篷 |
| 0x33 | TiltOnlyPergola | 仅倾斜凉亭 |
| 0x34 | SwingingShutter | 摆动百叶帘 |
| 0x35 | SlidingShutter | 滑动百叶帘 |
| 0xFF | Unknown | 未知 |

## WindSettingEnum8

该枚举定义 WindSetting Trait 的 Present Value 有效取值。

| 取值 | 名称 | 说明 |
| --- | --- | --- |
| 0x00 | Normal | 正常 |
| 0x01 | Sleep | 睡眠 |
| 0x02 | Natural | 自然 |

## ZigbeeDiagnosticsTraitPresentValueStructure

该结构体定义 ZigbeeDiagnostics Trait 的 Present Value 字段布局。

| 结构体成员序号 | 字段名 | 类型 | 必须 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | Channel | uint8 | M | Zigbee radio channel。 |
| 1 | ExtendedPANID | uint64 | M | Zigbee extended PAN identifier。 |
| 2 | NetworkKey | octetString | M | Zigbee network key。 |
| 3 | NetworkName | characterString | M | Zigbee network name。 |
| 4 | PANID | uint16 | M | Zigbee PAN identifier。 |
| 5 | RSSI | int8 | O | RSSI for the Zigbee network client in dBm。 |
| 6 | LQI | uint8 | O | Link quality indicator。 |
