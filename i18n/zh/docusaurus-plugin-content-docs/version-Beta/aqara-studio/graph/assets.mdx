# 素材库

## 概述

【内容待定】

## 通用属性

组件和素材在大屏设计中具有一系列通用属性，这些属性包括样式（如尺寸、位置、颜色等）、数据（如数据源绑定与映射）、动作（如点击事件等行为配置）等，便于统一管理和灵活配置。详细内容请参

## 素材类型

- 单组件素材（当用户更改了组件的样式后，就可以设置为素材）
- 组合素材
- 图片
- 图标

## 设置为素材

只可以保留样式，数据需要额外添加。

- 设置为单组件素材
- 设置为组合素材

## 上传图片和图标

Aqara Studio 支持您上传 PNG、JPG 和 GIF 格式的图片，单个文件大小不超过 2MB。

